---
title: Wiki Schema
description: 页面类型、目录结构和标签体系
tags: [meta]
created: 2026-05-07
updated: 2026-08-12
---

# Schema

## 目录结构

按 [[00-知识地图|知识地图]] 的六部分重要性排序组织：

| 目录 | MOC 章节 | 用途 | 页面数 |
|------|----------|------|--------|
| `wiki/01-基础/` | 一 | 生物学事实、范式框架、核心术语、运动史 | 7 |
| `wiki/02-批判分析/` | 二 | 政治经济学批判、意识形态分析 | 18 |
| `wiki/03-历史脉络/` | 三 | 历史脉络——"正常"的发明及其抵抗 | 6 |
| `wiki/04-实践应用/` | 四 | 体验、研究、教育与方法论——范式如何转化为行动 | 13 |
| `wiki/05-诊断与现象/` | 五 | 神经发育类型与相关心理现象——ASD、ADHD、述情障碍等 | 11 |
| `wiki/06-测试/` | 六 | 量表、测试工具与相关文献——与神经多样性理论相对隔离 | 27 |
| `wiki/authors/` | 七 | 作者 | 124 |
| `wiki/works/` | 八 | 参考文献 | 105 |

入口页面：[[00-知识地图]]

## Sources 目录组织

`sources/` 根目录是待处理文件的**收件箱**。完成摄取后，原始文件移入对应子文件夹。

| 目录 | 用途 |
|------|------|
| `sources/books/` | 专著、编辑卷（.epub, .pdf, .md 衍生阅读版） |
| `sources/papers/` | 期刊论文、会议论文（.pdf） |
| `sources/blog-posts/` | Substack、博客等网络文章（.md） |
| `sources/articles/` | 翻译文章、原创中文文章、其他非期刊写作（.md） |
| `sources/tests/` | 测试/量表相关的文献（期刊论文、PDF）——与神经多样性理论文献隔离 |
| `sources/YYYY-MM-DD/` | 按日期归档的摄取副本——摄入时自动创建，不可修改 |

### 工作流

1. 新文件直接放入 `sources/` 根目录（收件箱）
2. 执行 `/ingest`：文件复制到 `sources/YYYY-MM-DD/`，创建/更新 wiki 页面
3. **EPUB 格式**：摄入后自动运行 `epub2md.py` 转换为 .md 衍生阅读版，与原始 .epub 放置在同一目录
4. 完成后，原始文件从根目录移入对应类别文件夹（如 `sources/books/`），更新路径以便下次阅读
5. `sources/YYYY-MM-DD/` 内的副本保持不变——它们作为 wiki frontmatter `sources:` 字段的引用目标

## 命名规范

- 中文内容使用连续中文作为文件名（如 `神经多样性教育.md`），不加分词符
- 英文概念保留英文原名，中文译名作为别名
- 作者页以连字符命名（如 `Robert-Chapman.md`）
- 著作页以 `AuthorYear.md` 格式命名（如 `Chapman2023.md`）
- 测试页以连字符命名（如 `AQ-Child.md`、`RAADS-R.md`）

## 页面结构

每个内容页面按以下顺序组织：

1. **Frontmatter**——必需字段见下，一律块状写法
2. **H1 标题**
3. **导语段**——H1 后第一段必须是含主语的完整句子，能独立传达页面主题；与 `description` 一致，供搜索结果与 HTML meta 使用
4. **Infobox（作者/作品/测试页）**——H1 后、导语前插入统一信息框（格式见下）
5. **正文**——以二级节组织；标题直接说明该节讨论的问题、关系或论点
6. **Related**——页面底部，按「相关概念 → 相关文献 → 作者」排序（见记忆约定）

> **注意**：页面目录（TOC）由部署端自动生成，**不要**在 md 中写入 `> [!toc]`——Obsidian 中可能显示为空框，且与部署端自动目录重复。

> **注意**：**导语段是页面唯一的顶部概述**——不设概览 callout：概念介绍由导语段承担，页面导航由部署端自动生成的目录承担。Infobox 之后的正文必须直接以导语段（作品页为引用行后导语段）开始，**不要在顶部保留成组的粗体键值行**（`**身份**：…`、`**机构**：…` 等）——这些结构化信息应收进 infobox，避免出现"Infobox 断裂成两块"的视觉问题。

### Infobox 格式

信息框统一使用 callout 形式，置于 H1 标题之后、导语段之前：

```markdown
> [!infobox] <姓名>
> **职业**：哲学家、学者
> **机构**：Sheffield Hallam University
> **主要领域**：神经多样性研究、政治哲学
> **代表著作**：[[Chapman2023]]、[[ChapmanFletcherWatson2026]]
```

```markdown
> [!infobox] Chapman (2023)
> **作者**：[[Robert-Chapman]]
> **出版年份**：2023
> **类型**：专著
> **出版社**：Pluto Press
```

```markdown
> [!infobox] CAT-Q
> **测量对象**：掩饰（补偿、伪装、融入）
> **题数**：25
> **适用年龄**：16 岁及以上
> **开发者**：Hull et al. (2019)
```

作者页可扩展字段：`职称`、`学位`、`主页`；作品页可扩展字段：`期刊`、`许可`、`翻译`；测试页可扩展字段：`题数`、`适用年龄`、`开发者`。**正文顶部不要重复 infobox 中的键值信息**——Infobox 后直接接导语段（作品页为引用行后导语段）。

信息框内容必须来自页面正文已有信息，不得新增事实。

### 小作品标记

正文（不含 frontmatter）少于约 500 字符的页面标记为小作品：`tags` 增加 `stub` 即可——**不在正文中添加 note 提示**，`stub` 标签已足以供维护与搜索识别。

### Meta 页豁免

`00-知识地图`、`阅读路线`、`术语翻译对照表`、`TEMPLATE` 等元页面不受 `sources` 与 `## Related` 要求约束。

## 写作风格

- 避免用“核心判断”“核心原则”“核心逻辑”“核心贡献”等泛化标签替代具体表述；标题应直接说明该节讨论的问题、关系或论点。
- 只有在确实需要区分主次且依据明确时才使用“核心”，不要把它作为摘要、标题或评价性句式的默认修饰语。
- 标题避免未定义的缩写：首次出现的缩写（如 ND、ASD、ABA）在标题或正文首次出现处写全称（神经多样性范式、孤独谱系障碍、应用行为分析），必要时在 `aliases` 中登记。

## 必需 Frontmatter

每个 Wiki 页面必须包含：

```yaml
---
title: 页面标题
description: 一句话摘要
aliases:
  - 英文/缩写  ← 首个为英文或缩写（决定部署 URL）
  - 中文别名
tags:
  - 类型标签
  - 领域标签
sources:
  - YYYY-MM-DD/源文件名
created: YYYY-MM-DD
updated: YYYY-MM-DD
---
```

**写法**：`aliases`、`tags`、`sources` 一律使用块状列表（每项一行、`- ` 开头），禁止行内数组写法（`[a, b]`）。

### 作品页（works/）额外要求

所有 `works/` 目录下的页面必须额外包含：

1. **Frontmatter `citation_apa`** — APA 7th 格式的完整引用：

```yaml
citation_apa: "Author, A. A. (Year). Title. Journal, Volume(Issue), Pages. https://doi.org/xx"
```

2. **正文引用行** — 标题下方紧跟 `引用：` 行，方便读者直接复制：

```markdown
# 标题

引用：Author, A. A. (Year). Title. Journal, Volume(Issue), Pages. https://doi.org/xx
```

## 标签体系

标签采用扁平列表格式（Obsidian 兼容），按以下维度组织：

### 页面类型（每个页面一个）

| 标签 | 说明 |
|------|------|
| `theory` | 元层次范式框架（01-基础/中的理论页） |
| `concept` | 概念与批判分析（01-基础/ 02-批判/ 04-实践/） |
| `event` | 历史事件/运动（03-历史/） |
| `author` | 人物页面（authors/） |
| `work` | 著作页面（works/） |
| `test` | 测试/量表页面（05-测试/） |

### 领域标签（可多个）

| 标签 | 说明 |
|------|------|
| `neurodiversity` | 神经多样性 |
| `autism` | 孤独谱系 |
| `capitalism` | 资本主义批判 |
| `education` | 教育 |
| `disability` | 残障 |

### 主题标签（可多个）

| 标签 | 说明 |
|------|------|
| `foundational` | 基础性/奠基性 |
| `historical` | 历史脉络 |
| `political` | 政治经济学批判 |
| `applied` | 实践应用 |
| `intersectionality` | 交叉性 |
| `critique` | 批判分析 |
| `methodology` | 方法论 |
| `stub` | 小作品——内容不足约 500 字符，待扩展（见「小作品标记」） |
